{  
   "id": "user",
   "$schema": "http://json-schema.org/draft-04/schema#",
   "description": "This schema represents personal data to identify users/manager of the system from where data sill be collected.",
   "type": "object",

   "definitions": {
	   "patient_identification": {  
   	  	  "$ref": "patient-identification-1.x.json"
	   },
	   "gender": {  
	      "$ref": "gender-1.x.json"
	   },
	   "created": {  
	      "$ref": "date-time-1.x.json"
	   },
	   "lastUpdate": {  
	      "$ref": "date-time-1.x.json"
	   }
   },

   "properties": {
   	   "patient_identification": {  
   	  	  "$ref": "#/definitions/patient_identification"
	   },
	   "gender": {  
	      "$ref": "#/definitions/gender"
	   },
	   "created": {  
	      "$ref": "#/definitions/date_time"
	   },
	   "lastUpdate": {  
	      "$ref": "#/definitions/date_time"
	   }
   },

   "required": [
   		"patient_identification",
   		"gender",
   		"created",
   		"lastUpdate"
   ]  
}